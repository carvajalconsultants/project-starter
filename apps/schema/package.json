{
  "name": "schema",
  "version": "0.0.1",
  "description": "Schema docs and migration scripts for this project.",
  "author": "Carvajal Consultants, Inc. <support@carvajalonline.com>",
  "dependencies": {
    "graphile-migrate": "^1.4.1"
  },
  "scripts": {
    "commit": "bun --env-file=.env graphile-migrate commit",
    "uncommit": "bun --env-file=.env graphile-migrate uncommit",
    "watch": "bun --env-file=.env graphile-migrate watch"
  }
}
